[build]
  command = "npm run build"
  publish = "dist/instant-brut/browser" # Vérifie bien le nom de ton projet ici

[[plugins]]
  package = "@netlify/angular-runtime"
  [plugins.inputs]
    disable_runtime = true # On désactive la partie problématique